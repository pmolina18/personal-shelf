# Absolute URL to the backend API (omit for local dev with Vite proxy)
VITE_API_BASE_URL=https://shelfd-api.onrender.com/api

# Absolute URL to the backend image endpoint (omit for local dev)
VITE_IMAGES_BASE_URL=https://shelfd-api.onrender.com
